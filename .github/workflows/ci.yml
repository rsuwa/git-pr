name: CI

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  shell:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y bats shellcheck
      - name: Syntax
        run: |
          bash -n git-pr
          bash -n install.sh
      - name: ShellCheck
        run: shellcheck git-pr install.sh
      - name: Tests
        run: bats test
